subscribe:
  post:
    summary: Subscribe to event
    operationId: subscribeEvent
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
    responses:
      "200":
        $ref: "../components/responses.yaml#/SubscribeSuccess"
