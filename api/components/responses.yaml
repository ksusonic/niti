SubscribeSuccess:
  description: Subscription success
  content:
    application/json:
      schema:
        type: object
        properties:
          success: { type: boolean }
          message: { type: string }
          participants_count: { type: integer }